apiVersion: v1
kind: Service
metadata:
  name: netflix-svc
  labels:
    app: my-app-np
spec:
  type: Nodeport
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: my-app-np
